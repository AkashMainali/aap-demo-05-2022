---
- hosts: all
  gather_facts: no
  tasks:

    - name: debug variables from previous template
      debug:
        var: "{{ linux_hostname }}"
    - name: debug variables from previous template
      debug:
        var: "{{l inux_ip_address }}"

    # - name: Configure the DNS server
    #   include_role:
    #     name: configure_dns_server